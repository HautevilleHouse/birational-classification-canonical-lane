# Identification Bridge

This note records the determining-class bridge for `birational geometry`. It fixes the endpoint representative, tracks `eps_coh`, and closes the final strict margin `M_BCL` in strict-coherence mode.
